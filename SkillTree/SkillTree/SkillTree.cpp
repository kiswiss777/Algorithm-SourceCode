#include<iostream>
#include<string>
// skill_trees_len은 skill_trees의 길이입니다.
int solution(char* skill, char* skill_trees[], size_t skill_trees_len) {
	int skLen = strlen(skill);
	char pSChar, cSTChar;
	i
	for (int i = 0; i < skill_trees_len; i++) {
		int cSTLen = strlen(skill_trees[i]);
		for (int j = 0; j < cSTLen; j++) {

		}
	}
	int answer = 0;
	return answer;
}